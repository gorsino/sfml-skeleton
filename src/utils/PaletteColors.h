#ifndef PALETTECOLOR_H
#define PALETTECOLOR_H

#include "SFML/Graphics/Color.hpp"

const auto TURQUOISE = sf::Color(26, 188, 156);
const auto GREEN_SEA = sf::Color(22, 160, 133);
const auto SUN_FLOWER = sf::Color(241, 196, 15);
const auto ORANGE = sf::Color(243, 156, 18);
const auto EMERALD = sf::Color(46, 204, 113);
const auto NEPHRITIS = sf::Color(39, 174, 96);
const auto CARROT = sf::Color(230, 126, 34);
const auto PUMPKIN = sf::Color(211, 84, 0);
const auto PETER_RIVER = sf::Color(52, 152, 219);
const auto BELIZE_HOLE = sf::Color(41, 128, 185);
const auto ALIZARIN = sf::Color(231, 76, 60);
const auto POMEGRANATE = sf::Color(192, 57, 43);
const auto AMETHYST = sf::Color(155, 89, 182);
const auto WISTERIA = sf::Color(142, 68, 173);
const auto CLOUDS = sf::Color(236, 240, 241);
const auto SILVER = sf::Color(189, 195, 199);
const auto WET_ASPHALT = sf::Color(52, 73, 94);
const auto MIDNIGHT_BLUE = sf::Color(44, 62, 80);
const auto CONCRETE = sf::Color(149, 165, 166);
const auto ASBESTOS = sf::Color(127, 140, 141);

#endif //PALETTECOLOR_H